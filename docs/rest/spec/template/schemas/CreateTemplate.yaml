type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ template ]
      attributes:
        type: object
        required:
          - name
          - body
        properties:
          name:
            type: string
            minLength: 1
            maxLength: 255
            example: My template
          body:
            type: object
            description: KonvaStageConfig snapshot object
