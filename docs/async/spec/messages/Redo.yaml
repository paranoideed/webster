name: redo
summary: Client moves head one step forward; server broadcasts to all room members
payload:
  schemaFormat: 'application/vnd.aai.asyncapi+yaml;version=3.0.0'
  schema:
    $ref: '../schemas/UndoRedoPayload.yaml'
examples:
  - name: redo example
    payload:
      head: 5
