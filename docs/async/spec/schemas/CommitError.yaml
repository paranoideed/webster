type: object
required: [message]
properties:
  message:
    type: string
    description: Human-readable error description
    example: "Commit failed, please retry"
  errors:
    type: array
    description: Validation errors (present only when changes are invalid)
    items:
      type: string
    example: ["[0] parentId must be a non-empty string"]
