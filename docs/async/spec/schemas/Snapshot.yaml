type: object
required: [version, body, created_at]
properties:
  version:
    type: integer
    description: Snapshot version index (0, 1, 2, …)
    example: 2
  body:
    $ref: './konva/KonvaStageConfig.yaml'
  created_at:
    type: string
    format: date-time
