type: object
required: [snapshot, commits]
properties:
  snapshot:
    $ref: './Snapshot.yaml'
  commits:
    type: array
    description: Commits after the snapshot (up to n commits)
    items:
      $ref: './Commit.yaml'
